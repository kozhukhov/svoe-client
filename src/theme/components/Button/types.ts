export enum Variant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}

export enum Size {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}

export enum Color {
  DEFAULT = 'default',
  DANGER = 'danger',
}
